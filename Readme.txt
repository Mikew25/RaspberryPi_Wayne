First ensure connection to the Internet

Install WGET if not already;

ArchLinux example: Sudo pacman -S wget

Use Wget to download these files

wget URL.tar.gz

Untar files:

tar zxvf filename.tar.gz

Install? ->

makepkg -s